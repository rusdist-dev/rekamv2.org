# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# the corrupt pre-migration .git, kept for reference only
.git.corrupt-backup/

# testing
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# Visual baseline: 33 MB of full-page PNGs, regenerable from site/ with
#   npm run baseline:shots
# for as long as the old site is still there. Capture them once more and
# archive them OUTSIDE the repo before site/ is deleted at cutover.
baseline/screens/

# env
.env
.env*.local

# misc
.DS_Store
*.pem
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
